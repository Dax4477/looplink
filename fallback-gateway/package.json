{
  "name": "looplink-fallback-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Free non-Cloudflare front door for LoopLink HTTP/WebSocket traffic.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
