# LoopLink Web v0.4.2

Fixes reconnect-attempt races and remote-close ping-pong.

Important: after publishing, close all old LoopLink tabs and reopen exactly one tab. Do not clear site storage if you want to preserve the existing web device identity.
